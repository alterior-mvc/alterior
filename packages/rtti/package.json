{
  "name": "@alterior/rtti",
  "version": "3.0.0-beta.84",
  "description": "Typescript transformer for emitting all type metadata",
  "main": "dist/index.js",
  "module": "dist.esm/index.js",
  "scripts": {
    "clean": "rimraf dist dist.esm",
    "build": "npm run clean && npm run build:cjs && npm run build:esm",
    "build:cjs": "tsc",
    "build:esm": "tsc -p ./tsconfig.esm.json",
    "test": "rimraf dist && npm run build:cjs && node dist/test"
  },
  "author": "Alterior Project",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.15",
    "chai": "^4.3.0",
    "razmin": "^1.0.4",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "tslib": "^2.1.0"
  }
}
